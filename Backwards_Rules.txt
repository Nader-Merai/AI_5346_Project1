RULES - BACKWARD CHAINING
10
    IF ARTS = YES AND
    IF COMMUN_OTHERS = YES AND
    IF WRITING = YES AND
        THEN PROFESSION = COMMUNICATIONS
20
    IF ARTS = YES AND
    IF COMMUN_OTHERS = YES AND
    IF WRITING = NO
        THEN PROFESSION = ENGLISH
30
    IF ARTS = YES AND
    IF COMMUN_OTHERS = NO AND
    IF PPL_FUNCTION = YES AND
    IF HUMAN_MIND = YES
        THEN PROFESSION = PSYCHOLOGY
40
    IF ARTS = YES AND
    IF COMMUN_OTHERS = NO AND
    IF PPL_FUNCTION = YES AND
    IF HUMAN_MIND = NO
        THEN PROFESSION = SOCIAL_SCIENCES_AND_LAW
50
    IF ARTS = YES AND
    IF COMMUN_OTHERS = NO AND 
    IF PPL_FUNCTION = NO AND
    IF FOOD = YES
        THEN PROFESSION = CULINARY
60
    IF ARTS = YES AND
    IF COMMUN_OTHERS = NO AND
    IF PPL_FUNCTION = NO AND
    IF FOOD = NO
        THEN PROFESSION = FINE_ARTS
70
    IF ARTS = NO AND
    IF STEM = YES AND
    IF MATH = YES AND 
    IF PROB_SOLVE = YES
        THEN PROFESSION = ENGINEERING
80
    IF ARTS = NO AND
    IF STEM = YES AND
    IF MATH = YES AND
    IF PROB_SOLVE = NO
        THEN PROFESSION = SCIENCE
90
    IF ARTS = NO AND
    IF STEM = YES AND
    IF MATH = NO AND
    IF HUMAN_BODY = YES
        THEN PROFESSION = MEDICINE
100
    IF ARTS = NO AND 
    IF STEM = YES AND
    IF MATH = NO AND
    IF HUMAN_BODY = NO
        THEN PROFESSION = GEOGRAPHY
110
    IF ARTS = NO AND
    IF STEM = NO AND
    IF HELP_OTHERS = YES 
    IF PHY_WORK = YES 
        THEN PROFESSION = REPANDMAIT
120
    IF ARTS = NO AND
    IF STEM = NO AND
    IF HELP_OTHERS = YES AND
    IF TEACHING = YES
        THEN PROFESSION = EDUCATION
130
    IF ARTS = NO AND
    IF STEM = NO AND
    IF HELP_OTHERS = YES AND
    IF TEACHING = NO
        THEN PROFESSION = HEALTH_CARE
140 
    IF ARTS = NO AND
    IF STEM = NO AND
    IF HELP_OTHERS = NO AND
    IF SELF_MOTIVATED = YES
        THEN PROFESSION = BUSINESS
150
    IF ARTS = NO AND
    IF STEM = NO AND
    IF HELP_OTHERS = NO AND
    IF SELF_MOTIVATED = NO
        THEN PROFESSION = AGRICULTURE
